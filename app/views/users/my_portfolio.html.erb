<h1>My Portfolio</h1>

<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <%= render 'users/search_stock' %>
      <%= render 'users/result' %>
      <% if @user_status %>
        <%= render 'users/stock_table' %>
      <% else %>
        <p>Your request to add Stock is still pending</p>
      <% end %>
    </div>
    <div class="col-sm-4"><%= render 'users/current_balance' %></div>
  </div>









